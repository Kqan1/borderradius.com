import { RegisterForm as Form } from '@/app/(auth)/signup/form';

export default function signup() {

    return (
        <div className="h-[calc(100vh-4rem)] flex items-center justify-center">
            <Form />
        </div>
    )
};